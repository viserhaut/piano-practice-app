// sheet-music.js
// VexFlow を使った楽譜表示（Phase 2 で実装予定）
// CDN: https://cdn.jsdelivr.net/npm/vexflow@4.2.2/build/cjs/vexflow.js

const SheetMusic = (function () {
  // Phase 2 で以下を実装:
  // - MIDIパース済みデータ → VexFlow 音符データへの変換
  // - 五線譜の描画（ト音記号 / ヘ音記号）
  // - 現在の playhead に合わせた音符のハイライト

  return {};
})();
